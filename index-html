<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IT Asset Management Dashboard</title>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background: #F4F6FA; color: #1A2C3E; line-height: 1.5; }
        .dashboard-container { max-width: 1600px; margin: 0 auto; padding: 24px 32px; }
        .header-section { margin-bottom: 32px; }
        .header-section h1 { font-size: 28px; font-weight: 600; color: #0A2647; display: flex; align-items: center; gap: 12px; }
        .header-section h1 i { color: #1E4A6B; font-size: 28px; }
        .header-section p { color: #5A6E7A; font-size: 14px; margin-top: 8px; }
        .stats-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 24px; margin-bottom: 32px; }
        .stat-card { background: white; border-radius: 20px; padding: 24px; border: 1px solid #E8ECF0; transition: all 0.2s; }
        .stat-card:hover { box-shadow: 0 8px 25px rgba(0,0,0,0.08); transform: translateY(-2px); }
        .stat-card.total-cost { border-top: 4px solid #1E4A6B; }
        .stat-card.total-items { border-top: 4px solid #0F4C3A; }
        .stat-header { display: flex; justify-content: space-between; margin-bottom: 16px; }
        .stat-label { font-size: 13px; font-weight: 500; text-transform: uppercase; color: #6B7A8A; }
        .stat-icon { font-size: 28px; opacity: 0.7; }
        .stat-card.total-cost .stat-icon { color: #1E4A6B; }
        .stat-card.total-items .stat-icon { color: #0F4C3A; }
        .stat-value { font-size: 32px; font-weight: 700; color: #1A2C3E; }
        .filter-section { background: white; border-radius: 16px; padding: 20px 24px; margin-bottom: 32px; border: 1px solid #E8ECF0; }
        .filter-label { font-size: 13px; font-weight: 600; text-transform: uppercase; color: #6B7A8A; margin-bottom: 12px; display: block; }
        .filter-buttons { display: flex; flex-wrap: wrap; gap: 8px; }
        .filter-btn { background: #F4F6FA; border: none; padding: 8px 18px; border-radius: 30px; font-size: 13px; font-weight: 500; cursor: pointer; transition: all 0.2s; color: #4A5C6C; }
        .filter-btn:hover { background: #E8ECF0; transform: translateY(-1px); }
        .filter-btn.active { background: #1E4A6B; color: white; box-shadow: 0 2px 8px rgba(30,74,107,0.3); }
        .chart-section { background: white; border-radius: 20px; padding: 20px 24px; margin-bottom: 32px; border: 1px solid #E8ECF0; }
        .chart-title { font-size: 16px; font-weight: 600; color: #1A2C3E; margin-bottom: 20px; display: flex; align-items: center; gap: 8px; }
        .chart-title i { color: #1E4A6B; }
        .chart-container { min-height: 380px; width: 100%; }
        .table-section { background: white; border-radius: 20px; border: 1px solid #E8ECF0; overflow: hidden; display: flex; flex-direction: column; max-height: 500px; }
        .table-header-toolbar { padding: 16px 20px; border-bottom: 1px solid #E8ECF0; background: #FAFBFD; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px; }
        .table-title { font-size: 14px; font-weight: 600; color: #1A2C3E; }
        .clear-filters-btn { background: #F4F6FA; border: 1px solid #E8ECF0; padding: 6px 14px; border-radius: 8px; font-size: 12px; font-weight: 500; cursor: pointer; color: #6B7A8A; }
        .clear-filters-btn:hover { background: #E8ECF0; color: #1A2C3E; }
        .table-wrapper { overflow-x: auto; overflow-y: auto; flex: 1; }
        table { width: 100%; border-collapse: collapse; font-size: 13px; min-width: 1000px; }
        th { background: #FAFBFD; padding: 16px 12px; text-align: left; font-weight: 600; color: #4A5C6C; border-bottom: 1px solid #E8ECF0; position: sticky; top: 0; }
        td { padding: 14px 12px; border-bottom: 1px solid #F0F2F5; color: #2C3E50; }
        tr:hover { background: #FAFBFD; }
        .filter-select { width: 100%; padding: 8px 10px; border: 1px solid #E8ECF0; border-radius: 10px; font-size: 12px; background: white; cursor: pointer; margin-top: 10px; }
        .filter-select:focus { outline: none; border-color: #1E4A6B; }
        .badge { padding: 4px 10px; border-radius: 30px; font-size: 11px; font-weight: 600; display: inline-block; }
        .badge.assigned { background: #DBEAFE; color: #1E40AF; }
        .badge.instock { background: #D1FAE5; color: #065F46; }
        .badge.broken { background: #FEE2E2; color: #991B1B; }
        .loading-state, .empty-state, .error-state { text-align: center; padding: 60px 40px; color: #8A9AAA; }
        .loading-state i, .empty-state i, .error-state i { font-size: 48px; margin-bottom: 16px; opacity: 0.5; }
        .btn-reset { background: #1E4A6B; color: white; border: none; padding: 8px 20px; border-radius: 8px; font-size: 13px; font-weight: 500; cursor: pointer; margin-top: 12px; }
        .btn-reset:hover { background: #0F3A55; }
        .dashboard-footer { margin-top: 32px; text-align: center; padding: 20px; color: #8A9AAA; font-size: 12px; border-top: 1px solid #E8ECF0; }
        @media (max-width: 768px) { .dashboard-container { padding: 16px; } .stat-value { font-size: 24px; } }
    </style>
</head>
<body>
<div class="dashboard-container">
    <div class="header-section">
        <h1><i class="fas fa-chart-pie"></i> IT Asset Management Dashboard</h1>
        <p>Real-time inventory tracking | Asset status & cost analysis</p>
    </div>

    <div class="stats-row">
        <div class="stat-card total-cost">
            <div class="stat-header"><span class="stat-label">Total Purchase Cost</span><i class="fas fa-dollar-sign stat-icon"></i></div>
            <div class="stat-value" id="total-cost-val">$0</div>
        </div>
        <div class="stat-card total-items">
            <div class="stat-header"><span class="stat-label">Total Assets</span><i class="fas fa-microchip stat-icon"></i></div>
            <div class="stat-value" id="total-items-val">0</div>
        </div>
    </div>

    <div class="filter-section">
        <span class="filter-label"><i class="fas fa-filter"></i> Filter by Status</span>
        <div class="filter-buttons" id="status-filters">
            <button class="filter-btn active" data-status="All">All Statuses</button>
            <button class="filter-btn" data-status="Assigned">Assigned</button>
            <button class="filter-btn" data-status="In Stock">In Stock</button>
            <button class="filter-btn" data-status="Broken/Damaged">Broken/Damaged</button>
        </div>
    </div>

    <div class="chart-section">
        <div class="chart-title"><i class="fas fa-chart-bar"></i> Cost Distribution by Asset Status</div>
        <div id="chart-status" class="chart-container" style="height: 380px;"></div>
    </div>

    <div class="table-section">
        <div class="table-header-toolbar">
            <span class="table-title"><i class="fas fa-table-list"></i> Asset Inventory</span>
            <button class="clear-filters-btn" id="clear-all-filters"><i class="fas fa-eraser"></i> Clear All Filters</button>
        </div>
        <div class="table-wrapper">
            <table id="inventory-table">
                <thead id="table-header"></thead>
                <tbody id="table-body"><tr><td colspan="7" class="loading-state"><i class="fas fa-spinner fa-pulse"></i> Loading inventory data...</td></tr></tbody>
            </table>
        </div>
    </div>

    <div class="dashboard-footer">
        <i class="fas fa-sync-alt"></i> Data loaded from embedded dataset | Last update: <span id="update-time">--</span>
    </div>
</div>

<script>
    google.charts.load('current', { packages: ['corechart'] });
    
    // ============================================================
    // EMBEDDED DATA - Sample of your inventory (first 200 records)
    // This ensures the dashboard works without external connections
    // ============================================================
    const inventoryData = [
        { assetTag: "PE-INV00763", model: "Lenovo ideapad 320-14ISK", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "", purchaseCost: 0 },
        { assetTag: "PE-INV00766", model: "LG Monitor", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "", purchaseCost: 0 },
        { assetTag: "PE-INV00347", model: "Lenovo ideapad L340-15IWL", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2020-10-20", purchaseCost: 590.32 },
        { assetTag: "PE-INV00458", model: "Samsung Screen", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "PE-INV00885", model: "Dell Vostro 3590", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-03-18", purchaseCost: 582.64 },
        { assetTag: "132", model: "Samsung Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "PE-INV00510", model: "Dell laptop", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "", purchaseCost: 0 },
        { assetTag: "PE-INV00544", model: "Samsung Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "PE-INV00368", model: "Dell inspiron 3501", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "", purchaseCost: 821.67 },
        { assetTag: "PE-INV00624", model: "Samsung Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "PE-INV00637", model: "Lenovo ideapad 330S-14IKB", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2019-03-12", purchaseCost: 383.79 },
        { assetTag: "PE-INV00710", model: "LG Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2019-05-15", purchaseCost: 135.59 },
        { assetTag: "PE-INV00861", model: "Dell inspiron 3501", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2022-06-01", purchaseCost: 864.15 },
        { assetTag: "PE-INV00889", model: "LG Screen", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-12-17", purchaseCost: 205.28 },
        { assetTag: "PE-INV00890", model: "Dell inspiron 3501", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2022-06-01", purchaseCost: 864.15 },
        { assetTag: "PE-INV00399", model: "Dell inspiron 3501", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2022-06-01", purchaseCost: 864.15 },
        { assetTag: "154", model: "Samsung Screen", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "PE-INV00738", model: "Dell Vostro 3590", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-03-18", purchaseCost: 582.64 },
        { assetTag: "156", model: "Samsung Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "157", model: "Laptop", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "", purchaseCost: 575.75 },
        { assetTag: "PE-INV00735", model: "Samsung Screen", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "2018-05-06", purchaseCost: 118.88 },
        { assetTag: "159", model: "HP 15-dy1024wm", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-06-02", purchaseCost: 575.75 },
        { assetTag: "PE-INV00753", model: "LG Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2019-03-12", purchaseCost: 102.16 },
        { assetTag: "PE-INV00369", model: "LG Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-12-17", purchaseCost: 205.28 },
        { assetTag: "164", model: "Dell inspiron 3501", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-12-20", purchaseCost: 821.67 },
        { assetTag: "PE-INV00692", model: "Dell inspiron 3501", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-12-20", purchaseCost: 821.67 },
        { assetTag: "PE-INV00703", model: "Dell inspiron 3501", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-12-20", purchaseCost: 821.67 },
        { assetTag: "PE-INV00485", model: "Dell Vostro 3590", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-03-18", purchaseCost: 582.64 },
        { assetTag: "PE-INV00594", model: "Dell Vostro 3590", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-03-18", purchaseCost: 582.64 },
        { assetTag: "172", model: "LG Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-12-17", purchaseCost: 205.28 },
        { assetTag: "PE-INV00727", model: "Desktop Screen", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "2018-07-27", purchaseCost: 110.98 },
        { assetTag: "PE-INV00525", model: "Dell Vostro 3590", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-03-18", purchaseCost: 582.64 },
        { assetTag: "PE-INV00433", model: "Dell inspiron 3501", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2022-01-15", purchaseCost: 864.65 },
        { assetTag: "176", model: "LG Screen", category: "Desktop Screen", status: "In Stock", company: "HappyNetSac", purchaseDate: "2021-12-17", purchaseCost: 205.28 },
        { assetTag: "179", model: "Dell inspiron 3501", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2022-06-01", purchaseCost: 864.15 },
        { assetTag: "PE-INV00721", model: "LG Monitor", category: "Desktop Screen", status: "Assigned", company: "HappyNetSac", purchaseDate: "2018-02-21", purchaseCost: 85.61 },
        { assetTag: "181", model: "Dell Vostro 3590", category: "Laptop", status: "Assigned", company: "HappyNetSac", purchaseDate: "2021-03-18", purchaseCost: 582.64 },
        { assetTag: "184", model: "Dell inspiron 3501", category: "Laptop", status: "In Stock", company: "HappyNetSac", purchaseDate: "2022-01-15", purchaseCost: 864.65 }
    ];

    let currentStatus = "All";
    let columnFilters = { assetTag: '', model: '', category: '', status: '', company: '', purchaseDate: '', purchaseCost: '' };
    let uniqueValues = { assetTag: [], model: [], category: [], status: [], company: [], purchaseDate: [], purchaseCost: [] };

    // Calculate unique values from embedded data
    function calculateUniqueValues() {
        const tempUnique = { assetTag: new Set(), model: new Set(), category: new Set(), status: new Set(), company: new Set(), purchaseDate: new Set(), purchaseCost: new Set() };
        inventoryData.forEach(item => {
            if (item.assetTag) tempUnique.assetTag.add(item.assetTag);
            if (item.model) tempUnique.model.add(item.model);
            if (item.category) tempUnique.category.add(item.category);
            if (item.status) tempUnique.status.add(item.status);
            if (item.company) tempUnique.company.add(item.company);
            if (item.purchaseDate && item.purchaseDate !== '') tempUnique.purchaseDate.add(item.purchaseDate);
            if (item.purchaseCost) tempUnique.purchaseCost.add(item.purchaseCost.toString());
        });
        uniqueValues.assetTag = Array.from(tempUnique.assetTag).sort();
        uniqueValues.model = Array.from(tempUnique.model).sort();
        uniqueValues.category = Array.from(tempUnique.category).sort();
        uniqueValues.status = Array.from(tempUnique.status).sort();
        uniqueValues.company = Array.from(tempUnique.company).sort();
        uniqueValues.purchaseDate = Array.from(tempUnique.purchaseDate).sort();
        uniqueValues.purchaseCost = Array.from(tempUnique.purchaseCost).sort((a,b) => parseFloat(a) - parseFloat(b));
    }

    function renderTableHeader() {
        const columns = [
            { key: 'assetTag', label: 'Asset Tag', values: uniqueValues.assetTag },
            { key: 'model', label: 'Model', values: uniqueValues.model },
            { key: 'category', label: 'Category', values: uniqueValues.category },
            { key: 'status', label: 'Status', values: uniqueValues.status },
            { key: 'company', label: 'Company', values: uniqueValues.company },
            { key: 'purchaseDate', label: 'Purchase Date', values: uniqueValues.purchaseDate },
            { key: 'purchaseCost', label: 'Purchase Cost', values: uniqueValues.purchaseCost }
        ];
        
        const thead = document.getElementById('table-header');
        thead.innerHTML = '';
        const headerRow = document.createElement('tr');
        
        columns.forEach(col => {
            const th = document.createElement('th');
            const label = document.createElement('div');
            label.textContent = col.label;
            label.style.marginBottom = '8px';
            label.style.fontWeight = '600';
            
            const select = document.createElement('select');
            select.className = 'filter-select';
            const allOption = document.createElement('option');
            allOption.value = '';
            allOption.textContent = 'All';
            select.appendChild(allOption);
            
            col.values.forEach(value => {
                const option = document.createElement('option');
                option.value = value;
                option.textContent = value.length > 30 ? value.substring(0, 27) + '...' : value;
                select.appendChild(option);
            });
            
            select.value = columnFilters[col.key] || '';
            select.onchange = (e) => { columnFilters[col.key] = e.target.value; refreshDisplay(); };
            
            th.appendChild(label);
            th.appendChild(select);
            headerRow.appendChild(th);
        });
        
        thead.appendChild(headerRow);
    }

    function renderStatusFilters() {
        const statusDiv = document.getElementById('status-filters');
        const btns = statusDiv.querySelectorAll('.filter-btn');
        btns.forEach(btn => {
            const status = btn.getAttribute('data-status');
            btn.classList.toggle('active', (currentStatus === 'All' && status === 'All') || currentStatus === status);
            btn.onclick = () => { currentStatus = status; refreshDisplay(); };
        });
    }

    function getBadgeClass(status) {
        if (status === 'Assigned') return 'assigned';
        if (status === 'In Stock') return 'instock';
        if (status === 'Broken/Damaged') return 'broken';
        return '';
    }

    function drawChart(statusMap, totalCost) {
        const chartDiv = document.getElementById('chart-status');
        
        try {
            if (!statusMap || statusMap.size === 0) {
                chartDiv.innerHTML = '<div class="empty-state" style="height: 380px; display: flex; flex-direction: column; align-items: center; justify-content: center;"><i class="fas fa-chart-simple"></i><h4>No data available for chart</h4><p>Try adjusting your filters to see cost distribution</p></div>';
                return;
            }
            
            chartDiv.innerHTML = '';
            
            const chartData = [['Status', 'Cost (USD)', { role: 'annotation', type: 'string' }]];
            const sortedStatuses = Array.from(statusMap.keys()).sort();
            for (let statusName of sortedStatuses) {
                const cost = statusMap.get(statusName);
                chartData.push([statusName, cost, '$' + cost.toFixed(2)]);
            }
            
            const dataTable = google.visualization.arrayToDataTable(chartData);
            
            let colorArray = [];
            for (let i = 1; i < chartData.length; i++) {
                const statusName = chartData[i][0];
                if (statusName === 'Assigned') colorArray.push('#3B82F6');
                else if (statusName === 'In Stock') colorArray.push('#10B981');
                else if (statusName === 'Broken/Damaged') colorArray.push('#EF4444');
                else colorArray.push('#6B7280');
            }
            
            const options = {
                title: 'Total Cost: $' + totalCost.toFixed(2),
                backgroundColor: 'transparent',
                colors: colorArray,
                legend: { position: 'top', alignment: 'center', textStyle: { color: '#4A5C6C', fontSize: 12 } },
                bar: { groupWidth: '60%' },
                chartArea: { width: '70%', height: '65%', left: '12%', top: '12%' },
                annotations: { alwaysOutside: true, textStyle: { fontSize: 11, color: '#1A2C3E', bold: true }, stem: { color: 'transparent' } },
                vAxis: { title: 'Cost (USD)', format: 'currency', minValue: 0, viewWindow: { min: 0 } },
                hAxis: { title: 'Status', slantedText: false }
            };
            
            const chart = new google.visualization.BarChart(chartDiv);
            chart.draw(dataTable, options);
        } catch (error) {
            chartDiv.innerHTML = '<div class="error-state" style="height: 380px; display: flex; flex-direction: column; align-items: center; justify-content: center;"><i class="fas fa-chart-line"></i><h4>Unable to display chart</h4><button class="btn-reset" onclick="refreshDisplay()">Try Again</button></div>';
        }
    }

    function refreshDisplay() {
        let filtered = inventoryData;
        if (currentStatus !== 'All') filtered = filtered.filter(item => item.status === currentStatus);
        
        filtered = filtered.filter(item => {
            if (columnFilters.assetTag && item.assetTag !== columnFilters.assetTag) return false;
            if (columnFilters.model && item.model !== columnFilters.model) return false;
            if (columnFilters.category && item.category !== columnFilters.category) return false;
            if (columnFilters.status && item.status !== columnFilters.status) return false;
            if (columnFilters.company && item.company !== columnFilters.company) return false;
            if (columnFilters.purchaseDate && item.purchaseDate !== columnFilters.purchaseDate) return false;
            if (columnFilters.purchaseCost && item.purchaseCost.toString() !== columnFilters.purchaseCost) return false;
            return true;
        });
        
        renderStatusFilters();
        
        const totalCost = filtered.reduce((s, i) => s + i.purchaseCost, 0);
        document.getElementById('total-cost-val').innerHTML = '$' + totalCost.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
        document.getElementById('total-items-val').innerText = filtered.length;
        document.getElementById('update-time').innerText = new Date().toLocaleString();
        
        if (filtered.length === 0) {
            document.getElementById('table-body').innerHTML = '<tr class="empty-state"><td colspan="7"><i class="fas fa-search"></i><h4>No items match your current filter criteria</h4><p>Try clearing one or more filters to see more results.</p><button class="btn-reset" id="reset-empty-filters">Clear All Filters</button></td></tr>';
            document.getElementById('reset-empty-filters').onclick = () => resetAllFilters();
            return;
        }
        
        const tbody = document.getElementById('table-body');
        let tableHtml = '';
        for (let item of filtered) {
            tableHtml += '<tr>';
            tableHtml += '<td>' + escapeHtml(item.assetTag) + '</td>';
            tableHtml += '<td>' + escapeHtml(item.model) + '</td>';
            tableHtml += '<td>' + escapeHtml(item.category) + '</td>';
            tableHtml += '<td><span class="badge ' + getBadgeClass(item.status) + '">' + escapeHtml(item.status) + '</span></td>';
            tableHtml += '<td>' + escapeHtml(item.company) + '</td>';
            tableHtml += '<td>' + escapeHtml(item.purchaseDate) + '</td>';
            tableHtml += '<td>$' + item.purchaseCost.toFixed(2) + '</td>';
            tableHtml += '</tr>';
        }
        tbody.innerHTML = tableHtml;
        
        const statusMap = new Map();
        for (let item of filtered) statusMap.set(item.status, (statusMap.get(item.status) || 0) + item.purchaseCost);
        drawChart(statusMap, totalCost);
    }

    function resetAllFilters() {
        for (let key in columnFilters) columnFilters[key] = '';
        document.querySelectorAll('.filter-select').forEach(select => select.value = '');
        currentStatus = 'All';
        refreshDisplay();
    }

    function escapeHtml(str) {
        if (!str) return '';
        return str.replace(/[&<>]/g, function(m) {
            if (m === '&') return '&amp;';
            if (m === '<') return '&lt;';
            if (m === '>') return '&gt;';
            return m;
        });
    }

    // Initialize
    calculateUniqueValues();
    renderTableHeader();
    renderStatusFilters();
    refreshDisplay();
    document.getElementById('clear-all-filters').onclick = resetAllFilters;
</script>
</body>
</html>
